import { TLUiAssetUrlOverrides } from "tldraw";

/**
 * assets are stored in the public folder or any URL
 */
export const customAssetUrls: TLUiAssetUrlOverrides = {
  icons: {
    "tool-idle": "/academic-cap.svg",
  },
};
